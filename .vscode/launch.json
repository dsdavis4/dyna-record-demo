{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "debug",
      "runtimeArgs": ["-r", "ts-node/register"],
      "args": ["index.ts"],
      "cwd": "${workspaceFolder}",
      "protocol": "inspector"
    }
  ]
}
